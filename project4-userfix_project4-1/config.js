// Global API base configuration.
// If your frontend is hosted separately (e.g., on LWS) and your backend is on Render,
// set this to your Render URL, e.g.:
//   window.API_BASE = 'https://your-service.onrender.com';
// If frontend and backend are on the same origin, leave it as empty string.
window.API_BASE = '';
